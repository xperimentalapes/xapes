{
  "name": "xapes",
  "version": "1.0.0",
  "description": "Xperimental Mutant Apes website",
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp -r index.html styles.css script.js public .vercel/output/static/ || true"
  }
}
